# === Server ===
NODE_ENV=production
PORT=8080

# === App ===
FRONTEND_URL=https://omsorgsplus.se

# === Database (MongoDB Atlas) ===
# Exempel (SRV): mongodb+srv://<user>:<pass>@<cluster-url>/<db>?retryWrites=true&w=majority
MONGO_URL=

# === Mail (Gmail App Password) ===
MAIL_USER=
MAIL_PASS=
